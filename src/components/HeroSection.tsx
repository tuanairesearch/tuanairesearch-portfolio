const HeroSection = () => {
    return (
        <section className="bg-gray-700 text-white p-20 flex-grow">
            <p>Đây là Hero Section</p>
        </section>
    );
}

export default HeroSection;